# Local Development (SQLite) - Leave these commented or use defaults
# Uncomment these lines if you want to use PostgreSQL locally

# Production (PostgreSQL on Koyeb)
# Get this from your Koyeb PostgreSQL service settings
# DATABASE_URL=postgresql://user:password@host:port/dbname

# Flask Configuration
FLASK_DEBUG=False
SECRET_KEY=your-super-secret-key-here-change-in-production

# Port (Koyeb will set this automatically)
PORT=5000
